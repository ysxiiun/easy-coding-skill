---
name: easy-coding
version: 1.0.1
description: EASY-CODING 编程助手技能 - 人机共创模式。触发词："帮我完成"、"帮我做"、"我有一个需求"、"帮我实现"、"帮我修改"、"帮我修复"、"帮我优化"、"ec"、"EC"、"easy-coding"。若用户消息开头包含 "#no-coding"，则当前轮跳过 skill 全部流程与约束。⚠️ 用户发现 AI 偏离规则时，请说："检查 skill 规则"或"你现在处于哪个阶段？"
triggers:
  - 帮我完成
  - 帮我做
  - 我有一个需求
  - 帮我实现
  - 帮我修改
  - 帮我修复
  - 帮我优化
  - ec
  - EC
  - easy-coding
---

# 🔴 核心约束（每轮对话必须遵守）

## `#no-coding` 单轮旁路规则

**优先级最高，先于所有触发词、阶段判断、工具阻断检查执行。**

- 若用户消息在开头写入 `#no-coding`，则当前轮立即退出 easy-coding 模式
- 当前轮**完全跳过** easy-coding 的全部约束与流程，包括但不限于：
  - 阶段标注 `[阶段：XXXX]`
  - INIT / ANALYSIS / WAITING_CONFIRM / IMPLEMENT / MEMORY_* / COMPLETE
  - 方案确认阻断
  - 记忆写入与流程重置规则
- `#no-coding` 仅对当前轮生效，下一轮恢复正常触发逻辑
- 判定条件为“用户消息开头包含 `#no-coding`”；若仅在正文中出现，不触发旁路

**命中 `#no-coding` 时的处理要求：**
- 不引用本 skill 的流程规范约束当前轮行为
- 直接按普通 agent 模式理解并处理用户请求
- 不输出“当前阶段禁止执行”之类的 skill 阻断文案

> 违反以下任何一条 = 任务失败

| 约束 | 违反示例 | 正确做法 |
|------|---------|---------|
| **禁止跳步** | 分析完需求直接写代码 | 必须按阶段顺序执行 |
| **方案未确认禁止执行** | 用户说"确认"前直接修改代码 | 输出 `[阶段：WAITING_CONFIRM]` 阻断等待 |
| **无论变更大小必须确认** | 以"改动很小"为由直接修改 | 即使改一个变量名也要出方案等待确认 |
| **每次回复必须标注阶段** | 直接输出方案，无阶段标识 | `[阶段：XXXX]` 开头 |
| **禁止自动推进** | 多轮讨论后未等用户确认就执行 | 必须等待用户明确说"确认/ok/开始" |

---

# ⚠️ 工具使用阻断检查（执行任何操作前必须检查）

**若当前轮命中 `#no-coding`，本节全部失效，直接跳过。**

**使用任何文件操作工具前，必须完成以下检查：**

```
□ 当前阶段是否为 IMPLEMENT？
  - 若否 → 禁止使用工具，输出"[阶段：XXX] 当前阶段禁止执行操作"
□ 用户是否已明确确认方案？
  - 若否 → 禁止使用工具，输出阻断提示等待确认
□ 本次操作是否在已确认的方案范围内？
  - 若否 → 必须重新出方案，返回 WAITING_CONFIRM
```

**绝对禁止（违反=任务失败）：**
- ❌ 在 INIT/ANALYSIS/WAITING_CONFIRM 阶段使用任何文件操作工具
- ❌ 未经用户明确确认就执行文件修改
- ❌ 以"改动很小"为由跳过确认直接使用工具

---

# 🛑 用户紧急停止指令

**若当前轮命中 `#no-coding`，本节全部失效，直接跳过。**

**用户说以下任一指令时，AI 必须立即停止当前操作：**
- "停止" / "不对" / "错了" / "你没按流程" / "检查 skill 规则"

**停止后的处理：**
1. 立即停止当前操作（即使工具已调用也不能执行）
2. 输出：`[阶段：INIT] 检测到紧急停止指令，流程重置`
3. 重新从阶段 1 开始执行

---

# 📋 阶段总览（完整工作流程）

**若当前轮命中 `#no-coding`，不进入下述任何阶段。**

```
┌─────────────────────────────────────────────────────────────┐
│  ① INIT → ② ANALYSIS → ③ WAITING_CONFIRM → ④ IMPLEMENT  │
│                              ↓                              │
│  ⑦ COMPLETE ← ⑥ MEMORY_LONG ← ⑤ MEMORY_SHORT            │
└─────────────────────────────────────────────────────────────┘

□ 禁止跳步  □ 每步标注阶段  □ 确认后执行  □ 变更重新确认
```

| 阶段 | 名称 | 进入条件 | 离开条件 | 关键约束 |
|:---:|------|---------|---------|---------|
| ① | INIT | skill 被触发 | 初始化完成/背景加载完成 | 项目未初始化禁止进入下一阶段 |
| ② | ANALYSIS | 用户描述需求 | 方案输出完成 | 需求不清晰禁止进入下一阶段 |
| ③ | WAITING_CONFIRM | 方案已输出 | 用户说"确认/ok/开始" | **用户未确认禁止进入下一阶段** |
| ④ | IMPLEMENT | 用户已确认方案 | 所有步骤完成 | 每步完成后必须汇报 |
| ⑤ | MEMORY_SHORT | 实施完成 | 记忆文件生成 | **⚠️ 自动触发，无需用户确认** |
| ⑥ | MEMORY_LONG | 短期记忆≥10 条 | 沉淀完成 | **⚠️ 条件满足时自动触发** |
| ⑦ | COMPLETE | 记忆处理完成 | 流程结束 | 输出完成报告 |

---

# 强制自我审查（每次回复前执行）

**若当前轮命中 `#no-coding`，本节全部失效，直接按普通 agent 模式响应。**

在生成任何内容前，AI 必须在内心完成：

```
□ 我现在处于哪个阶段？（INIT/ANALYSIS/WAITING_CONFIRM/IMPLEMENT/MEMORY_SHORT/MEMORY_LONG/COMPLETE）
□ 当前阶段的核心约束是什么？
□ 我是否即将违反"方案未确认禁止执行"规则？
□ 我是否即将违反"禁止跳步"规则？
□ 我是否即将在错误阶段使用工具？
□ 本次回复是否已包含阶段标注 [阶段：XXXX]？
```

**若任一检查不通过 → 停止生成 → 输出阻断提示 → 等待用户确认**

---

## 【阶段 1】INIT - 初始化

**进入条件**：skill 被触发

### 1.1 初始化检查

**🔴 只认 `.easy-coding/` 目录，其他目录完全无视**

**检测步骤：**
1. 使用 `list_dir` 检查项目根目录
2. 检查 `.easy-coding/` 目录及关键文件是否存在

**自检清单：**
```
□ .easy-coding/ 目录存在
□ .easy-coding/memory/short/ 目录存在
□ .easy-coding/memory/long/ 目录存在
□ .easy-coding/SOUL.md 存在且已填充
□ .easy-coding/RULES.md 存在且已填充
□ .easy-coding/ABSTRACT.md 存在且已填充
□ .easy-coding/memory/long/MEMORY.md 存在
```

**检查结果处理：**

**情况 A：全部存在且有效**
→ 进入 1.2 输出背景摘要

**情况 B：有缺失项**
→ 列出缺失明细，请求用户确认初始化：

```
[阶段：INIT]

⚠️ 项目初始化不完全

缺失项明细：
1. ❌ .easy-coding/SOUL.md - 未创建
2. ❌ .easy-coding/RULES.md - 已创建但未填充
3. ❌ .easy-coding/ABSTRACT.md - 未创建
...

是否开始初始化？（回复"确认"开始，将执行 flow/init.md 中的初始化流程）
```

**用户确认后：**
- 渐进式加载 `flow/init.md`
- 执行完整初始化流程（目录创建、SOUL/RULES/ABSTRACT/MEMORY 初始化）
- 完成后返回本文件继续

### 1.2 输出背景摘要（已初始化时）

```
[阶段：INIT]

✅ 背景加载完成
- 项目架构：{已读取/已初始化}
- 历史记忆：{N}条短期记忆
- 编码规范：{已加载}

请描述您的需求，我将为您分析技术方案。
```

---

## 【阶段 2】ANALYSIS - 需求分析

**进入条件**：用户已描述需求

### 2.1 需求清晰度检查（必须先执行）

**检查清单（满足≥4 项可进入方案设计）：**

| # | 检查项 | 追问话术 |
|---|--------|---------|
| 1 | 目标用户/使用场景 | "这个功能的使用场景是？" |
| 2 | 输入/输出格式 | "期望的输入和输出是什么？" |
| 3 | 边界条件（做什么&不做什么） | "有哪些情况不需要处理？" |
| 4 | 涉及模块 | "是否涉及 XX 模块的修改？" |
| 5 | 约束条件（性能/安全/兼容性） | "有性能或兼容性要求吗？" |

**执行逻辑：**
1. 逐项检查，标记为"已知"或"未知"
2. 若"未知"≥2 项 → 输出追问列表
3. 用户回答后重新检查
4. 满足条件后进入方案设计

**追问输出格式：**
```
[阶段：ANALYSIS]

需要澄清以下问题以便设计方案：

1. **使用场景**：这个功能的使用场景是？
2. **边界条件**：是否需要兼容旧版本数据？

请补充以上信息，我将为您输出技术方案。
```

### 2.2 加载项目背景数据

**⚠️ 强制要求：此步骤必须执行，不可跳过**

无论是否为首次执行，进入 ANALYSIS 阶段后**必须重新加载**以下文件：

**必须读取以下文件建立完整上下文：**
```
□ .easy-coding/SOUL.md          - 团队约定与核心原则
□ .easy-coding/RULES.md         - 编码规范
□ .easy-coding/ABSTRACT.md      - 项目架构摘要
□ .easy-coding/memory/long/MEMORY.md  - 长期记忆（若存在）
□ .easy-coding/memory/short/*.md      - 所有短期记忆（按序号排序）
```

**读取后提炼：**
- 项目架构：核心模块、技术栈、目录索引
- 历史决策：与本次任务相关的架构决策、技术选型
- 可复用经验：类似问题的解决方案
- 业务规则：相关的业务约束
- 近期任务：最近 1-3 条短期记忆的内容

### 2.3 分析维度

- 任务类型：新功能/Bug 修复/重构/性能优化
- 业务子域：属于哪个核心模块
- 需求边界：明确"做什么"和"不做什么"
- 关联影响：是否涉及历史记忆中的相关模块
- 规范约束：RULES.md 中的【强制】规范是否相关
- 可复用性：是否有历史经验可直接复用

**若需求不清晰 → 主动追问，禁止猜测**
**实现步骤要具体到代码维度，在哪个文件修改哪行代码要表达清楚**

### 2.4 输出技术方案（强制格式）

```markdown
[阶段：ANALYSIS]

## 技术方案：{任务标题}

### 任务类型
{新功能/Bug 修复/重构/性能优化}

### 需求摘要
{2-3 句话描述}

### 背景数据应用
- **相关架构**：{引用 ABSTRACT.md 中的相关模块}
- **历史决策**：{引用长期记忆中相关的技术决策}
- **可复用经验**：{引用历史经验，若无则填"无"}
- **规范约束**：{引用 RULES.md 中相关的【强制】规范}

### 影响面分析
- 涉及模块：{列表}
- 核心类：{列表}
- 数据库变更：{有/无}
- 接口变更：{有/无}
- 关联历史任务：{相关的短期记忆序号，若无则填"无"}

### 实现步骤
1. {步骤 1}
2. {步骤 2}
...

### 风险与注意事项
- {风险 1}
- {风险 2}
```

---

## 【阶段 3】WAITING_CONFIRM - 等待确认（阻断状态）

**进入条件**：方案已输出
**离开条件**：用户明确说"确认/ok/开始/没问题/执行"

### 3.1 强制阻断提示

```
[阶段：WAITING_CONFIRM] ⏸️ 阻断状态

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
以上为技术方案，请确认或提出修改意见。

✅ 确认方式：回复"确认" / "ok" / "开始" / "没问题"
✏️ 修改方式：直接告知修改意见，我将更新方案

⚠️ 在您明确确认前，我绝对不会执行任何代码变更。
⚠️ 即使多轮讨论，我也必须等待您的明确确认指令。
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 3.2 阻断条件检查

**满足以下任一才能进入阶段 4：**
- [ ] "确认" / "ok" / "开始" / "没问题" / "执行"
- [ ] "就这样" / "可以了" / "done"

**不满足的情况（继续等待）：**
- 用户提出修改 → 更新方案 → **重新进入 WAITING_CONFIRM**
- 用户追问问题 → 回答问题 → **重新进入 WAITING_CONFIRM**
- 用户未明确表态 → 继续等待，再次提醒确认

---

## 【阶段 4】IMPLEMENT - 技术实现

**进入条件**：用户已明确确认方案

### 4.1 实施前声明

```
[阶段：IMPLEMENT]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚀 开始技术实施

确认状态：✅ 用户已确认
实施方案：{方案标题}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 4.2 实施前必须加载

- `.easy-coding/RULES.md` - 确保编码规范始终生效

### 4.3 按步骤实施

**每完成一步必须输出：**
```
[阶段：IMPLEMENT]

✅ 步骤 {N}/{总} 完成：{步骤描述}
   文件：{文件路径}
   变更：{简要说明}
   规范检查：{是否遵守 RULES.md 中的【强制】规范}
```

**编码规范约束：**
- 所有代码必须遵守 `RULES.md` 中的【强制】规范
- 若因特殊情况需要违反，必须说明原因并获得用户同意
- 若 `RULES.md` 中无相关规范，按通用最佳实践执行

### 4.4 实施中的变更控制

**⚠️ 绝对约束：无论变更大小，必须重新出方案并等待用户确认**

实施过程中，如果用户提出任何变更（哪怕是修改一个变量名、调整一行代码）：

1. **立即停止当前实施**
2. **输出变更方案**：说明变更内容、影响范围、修改方式
3. **返回 WAITING_CONFIRM 阶段**：等待用户明确确认
4. **用户确认后**：按新方案继续实施

**禁止行为（违反即任务失败）：**
- ❌ 未经确认擅自执行任何代码变更
- ❌ 以"改动很小"为由跳过确认流程
- ❌ 在汇报中隐藏已执行的变更

### 4.5 实施完成与等待用户确认

**输出完成报告：**

```
[阶段：IMPLEMENT]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 技术实施完成

变更文件汇总：
1. {文件路径}：{变更说明}
2. {文件路径}：{变更说明}

EASY-CODING 已完成代码编写，请您检查代码变动，如有不妥，请发送指令给我，如无问题请确认。

✅ 确认方式：回复"确认" / "ok" / "开始" / "没问题"
✏️ 修改方式：直接告知修改意见

⚠️ 确认后将自动执行：短期记忆生成 → 长期记忆沉淀
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**用户确认后（自动执行，无需额外确认）：**
1. 自动进入 [阶段：MEMORY_SHORT]
2. 自动生成短期记忆文件
3. 自动进入 [阶段：MEMORY_LONG]
4. 自动执行长期记忆沉淀（如满足条件）
5. 输出 [阶段：COMPLETE]

---

## 【阶段 5】MEMORY_SHORT - 短期记忆生成 ⚠️ 必须执行

> 🔄 此步骤自动触发，无需用户确认

**进入条件**：实施完成

### 5.1 生成记忆文件

**文件名规则**：`{序号}_{日期}_{智能命名}.md`
- 序号：3 位数字，从 001 开始递增
- 示例：`001_20260324_新增用户登录功能.md`

**序号获取**：扫描 `.easy-coding/memory/short/`，取最大序号 +1

### 5.2 记忆内容格式

```markdown
# {智能命名}

> 序号：{XXX}
> 日期：{YYYY-MM-DD HH:MM}
> 任务类型：{新功能/Bug 修复/重构/性能优化}

## 任务概述
{2-3 句话}

## 技术方案摘要
{核心思路，100 字内}

## 变更内容
| 文件 | 变更类型 | 说明 |
|---|---|---|
| {路径} | {新增/修改/删除} | {说明} |

## 遇到的问题与解决方案
{若有，记录；无则填"无"}

## 技术决策记录
{若有，记录；无则填"无"}

## 未尽事项
{若有 TODO；无则填"无"}

## 关联记忆
- 前置：{前置任务序号}
- 后续：{后续任务序号}
```

### 5.3 短期记忆生成完成

```
[阶段：MEMORY_SHORT]

✅ 短期记忆已生成：{文件名}

即将自动执行：长期记忆沉淀检查（若短期记忆超过 10 条则自动沉淀）
```

---

## 【阶段 6】MEMORY_LONG - 长期记忆沉淀 ⚠️ 条件触发

> 🔄 短期记忆≥10 条时自动触发，无需用户确认

**进入条件**：短期记忆生成完成后**自动触发**（若短期记忆总数≥10 条），或用户主动触发

**主动触发方式**：用户说"沉淀记忆" / "整理记忆" / "归档记忆"

### 6.1 沉淀范围

**自动沉淀条件：**
- 扫描 `.easy-coding/memory/short/` 目录
- 若短期记忆文件总数 **≥ 10 条** → 触发自动沉淀
- 处理最早的若干条，使剩余短期记忆 ≤ 5 条

**处理对象**：最早的短期记忆文件（按序号排序）

**判断方法：**
- 按序号从小到大排序（001, 002, 003...）
- 保留最新的 5 条
- 将超出的（第 6 条及更早的）纳入沉淀范围

### 6.2 执行步骤

1. 扫描 `.easy-coding/memory/short/` 目录
2. 统计短期记忆文件总数
3. **自动沉淀逻辑：**
   - 若总数 < 10 条 → 输出"短期记忆不足 10 条，暂不沉淀" → 进入 COMPLETE
   - 若总数 ≥ 10 条 → 按序号排序，保留最新的 5 条，将超出的纳入沉淀范围
4. 读取符合条件的短期记忆，提取核心信息：
   - 架构决策
   - 技术选型
   - 可复用经验
   - 业务规则
5. 合并到 `.easy-coding/memory/long/MEMORY.md`
6. **删除已沉淀的短期记忆文件**
7. 输出沉淀摘要

### 6.3 长期记忆内容规范

**长度限制：≤ 5000 字符**

**超出时淘汰规则：**
- 优先淘汰最旧的"已淘汰记录"
- 其次淘汰最旧的"可复用经验"
- 最后淘汰最旧的"业务规则"
- **架构决策和技术选型永久保留**（除非被新的决策覆盖）

### 6.4 输出

**有沉淀时：**
```
[阶段：MEMORY_LONG]

📚 长期记忆已更新
- 归档短期记忆：{N} 条（创建超过 1 天）
- 更新章节：
  - 架构决策：{X} 条
  - 技术选型：{Y} 条
  - 可复用经验：{Z} 条
  - 业务规则：{W} 条
- 已删除短期记忆文件：{文件名列表}
- 当前长期记忆长度：{L} 字符 / 5000 字符

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎉 本次任务流程全部完成！

阶段回顾：
✅ INIT - 初始化
✅ ANALYSIS - 需求分析
✅ WAITING_CONFIRM - 方案确认
✅ IMPLEMENT - 技术实现
✅ MEMORY_SHORT - 短期记忆生成
✅ MEMORY_LONG - 长期记忆沉淀
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[阶段：COMPLETE]
```

**无沉淀时：**
```
[阶段：MEMORY_LONG]

📚 长期记忆检查完成
- 短期记忆共 {N} 条，不足 10 条，暂不沉淀
- 长期记忆未更新

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎉 本次任务流程全部完成！

阶段回顾：
✅ INIT - 初始化
✅ ANALYSIS - 需求分析
✅ WAITING_CONFIRM - 方案确认
✅ IMPLEMENT - 技术实现
✅ MEMORY_SHORT - 短期记忆生成
✅ MEMORY_LONG - 长期记忆沉淀（无需处理）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[阶段：COMPLETE]
```

---

## 【阶段 7】COMPLETE - 完成

**进入条件**：长期记忆沉淀完成

**输出：**
```
[阶段：COMPLETE]

🎉 任务全部完成！

如需继续新任务，请描述需求。
```

---

# 🔄 阶段流转与中断处理

## 标准流转（单向）

> 流程图见上方「📋 阶段总览」

## 中断处理（用户主动）

| 用户行为 | AI 响应 | 阶段跳转 |
|---------|--------|---------|
| "需求变了" / "重新想一下" | 停止当前操作，清空当前方案 | → ANALYSIS |
| "等等，我再看下" | 暂停等待，不输出方案 | 保持当前阶段 |
| "换个方案" / "有其他方式吗" | 输出备选方案 | → WAITING_CONFIRM |
| "检查 skill 规则" / "错了" | 立即停止，重置 | → INIT |
| 实施中变更请求 | 停止实施，输出变更方案 | → WAITING_CONFIRM |

## 中断输出规范

```
[阶段：ANALYSIS]（重置）

检测到需求变更，已清空原方案。

请描述变更后的需求，我将重新分析。
```

---

# 📎 附录：模板文件

模板文件位于 `templates/` 目录，初始化时自动拷贝到 `.easy-coding/`：

- `templates/SOUL.md` → `.easy-coding/SOUL.md`
- `templates/RULES.md` → `.easy-coding/RULES.md`
- `templates/ABSTRACT.md` → `.easy-coding/ABSTRACT.md`
- `templates/MEMORY.md` → `.easy-coding/memory/long/MEMORY.md`
