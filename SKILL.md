---
name: easy-coding
description: EASY-CODING 编程助手技能。触发词："帮我完成"、"帮我做"、"我有一个需求"、"帮我实现"、"帮我修改"、"帮我修复"、"帮我优化"、"ec"、"EC"、"easy-coding"。⚠️ 用户发现AI偏离规则时，请说："检查skill规则"或"你现在处于哪个阶段？"
triggers:
  - 帮我完成
  - 帮我做
  - 我有一个需求
  - 帮我实现
  - 帮我修改
  - 帮我修复
  - 帮我优化
  - ec
  - EC
  - easy-coding
---

# 🔴 核心约束（每轮对话必须遵守）

> 违反以下任何一条 = 任务失败

1. **禁止跳步** - 必须按阶段顺序执行，不能跳过任何阶段
2. **方案未确认禁止执行** - 用户明确说"确认/ok/开始"前，禁止任何代码变更
3. **无论变更大小必须确认** - 即使是修改一个变量名，也必须重新出方案等待确认
4. **每次回复必须标注当前阶段** - 格式：`[阶段: XXXX]`
5. **禁止自动推进** - 多轮讨论后仍需等待用户明确确认，不能自动执行

---

# 强制自我审查（每次回复前执行）

在生成任何内容前，AI 必须在内心完成：

```
□ 我现在处于哪个阶段？（INIT/ANALYSIS/WAITING_CONFIRM/IMPLEMENT/MEMORY_SHORT/COMPLETE）
□ 当前阶段的核心约束是什么？
□ 我是否即将违反"方案未确认禁止执行"规则？
□ 我是否即将违反"禁止跳步"规则？
□ 本次回复是否已包含阶段标注 [阶段: XXXX]？
```

**若任一检查不通过 → 停止生成 → 输出阻断提示 → 等待用户确认**

---

# 五阶段工作流

## 阶段定义

| 阶段 | 名称 | 进入条件 | 阻断规则 |
|---|---|---|---|
| INIT | 初始化 | skill被触发 | 项目未初始化完成禁止进入下一阶段 |
| ANALYSIS | 需求分析 | 用户描述需求 | 需求不清晰禁止进入下一阶段 |
| WAITING_CONFIRM | 等待确认 | 方案已输出 | **用户未说"确认/ok/开始"禁止进入下一阶段** |
| IMPLEMENT | 技术实现 | 用户已确认方案 | 每步完成后必须汇报 |
| MEMORY_SHORT | 短期记忆 | 实施完成 | 生成短期记忆文件 |
| MEMORY_LONG | 长期记忆 | 短期记忆生成后自动触发 | 处理超过1天的短期记忆 |
| COMPLETE | 完成 | 长期记忆沉淀完成 | 流程结束 |

---

## 【阶段1】INIT - 初始化

**进入条件**：skill被触发

### 1.1 检测旧版本

**检查是否存在旧版本目录：**
- `.fliggy-coding/` - 旧版本（fliggy-coding）的目录

**若存在旧版本：**
```
[阶段: INIT]

⚠️ 检测到旧版本配置目录：.fliggy-coding/

EASY-CODING 使用新的目录结构（.easy-coding/），不兼容旧版本。

请选择处理方式：
1. 【推荐】迁移旧数据：将旧记忆文件迁移到新目录（需用户确认）
2. 重新初始化：创建全新的 .easy-coding/ 目录（旧数据保留但不使用）

请输入您的选择（1 或 2）：
```

**迁移逻辑（用户选择1）：**
1. 创建 `.easy-coding/` 目录结构
2. 将 `.fliggy-coding/memory/` 中的文件按新命名规则迁移
3. 重新生成 `ABSTRACT.md`、`SOUL.md`、`RULES.md`
4. 告知用户迁移完成

**重新初始化（用户选择2）：**
1. 创建全新的 `.easy-coding/` 目录
2. 保留 `.fliggy-coding/` 但不使用
3. 执行全新初始化流程

### 1.2 强制自检清单

```
□ .easy-coding/ 目录存在
□ .easy-coding/memory/ 目录存在
□ .easy-coding/memory/short/ 目录存在
□ .easy-coding/memory/long/ 目录存在
□ .easy-coding/SOUL.md 存在
□ .easy-coding/RULES.md 存在
□ .easy-coding/ABSTRACT.md 存在
```

**缺失项自动创建**（从 templates/ 目录拷贝模板文件）

### 1.3 项目初始化（ABSTRACT.md 不存在时）

**阻断声明：此步骤未完成前，禁止进入任何后续阶段。**

1. 告知用户："本项目首次使用 EASY-CODING，正在分析项目架构..."
2. `list_dir` 扫描项目结构
3. 读取核心源码，理解架构
4. 生成 `ABSTRACT.md`
5. 向用户简述架构，询问："架构分析是否准确？确认后开始您的任务。"
6. **等待用户明确回复"确认"后，才进入阶段2**

### 1.3 输出背景摘要

```
[阶段: INIT]

✅ 背景加载完成
- 项目架构：{已读取/已初始化}
- 历史记忆：{N}条短期记忆
- 编码规范：{已加载}

请描述您的需求，我将为您分析技术方案。
```

---

## 【阶段2】ANALYSIS - 需求分析

**进入条件**：用户已描述需求

### 2.1 加载项目背景数据

**必须读取以下文件建立完整上下文：**

```
□ .easy-coding/SOUL.md          - 团队约定与核心原则
□ .easy-coding/RULES.md         - 编码规范
□ .easy-coding/ABSTRACT.md      - 项目架构摘要
□ .easy-coding/memory/long/MEMORY.md  - 长期记忆（若存在）
□ .easy-coding/memory/short/*.md      - 所有短期记忆（按序号排序）
```

**读取后提炼：**
- 项目架构：核心模块、技术栈、目录索引
- 历史决策：与本次任务相关的架构决策、技术选型
- 可复用经验：类似问题的解决方案
- 业务规则：相关的业务约束
- 近期任务：最近1-3条短期记忆的内容

### 2.2 分析维度

- 任务类型：新功能/Bug修复/重构/性能优化
- 业务子域：属于哪个核心模块
- 需求边界：明确"做什么"和"不做什么"
- 关联影响：是否涉及历史记忆中的相关模块
- 规范约束：RULES.md 中的【强制】规范是否相关
- 可复用性：是否有历史经验可直接复用

**若需求不清晰 → 主动追问，禁止猜测**

### 2.3 输出技术方案（强制格式）

```markdown
[阶段: ANALYSIS]

## 技术方案：{任务标题}

### 任务类型
{新功能/Bug修复/重构/性能优化}

### 需求摘要
{2-3句话描述}

### 背景数据应用
- **相关架构**：{引用 ABSTRACT.md 中的相关模块}
- **历史决策**：{引用长期记忆中相关的技术决策}
- **可复用经验**：{引用历史经验，若无则填"无"}
- **规范约束**：{引用 RULES.md 中相关的【强制】规范}

### 影响面分析
- 涉及模块：{列表}
- 核心类：{列表}
- 数据库变更：{有/无}
- 接口变更：{有/无}
- 关联历史任务：{相关的短期记忆序号，若无则填"无"}

### 实现步骤
1. {步骤1}
2. {步骤2}
...

### 风险与注意事项
- {风险1}
- {风险2}
```

---

## 【阶段3】WAITING_CONFIRM - 等待确认（阻断状态）

**进入条件**：方案已输出
**离开条件**：用户明确说"确认/ok/开始/没问题/执行"

### 3.1 强制阻断提示

```
[阶段: WAITING_CONFIRM] ⏸️ 阻断状态

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
以上为技术方案，请确认或提出修改意见。

✅ 确认方式：回复"确认" / "ok" / "开始" / "没问题"
✏️ 修改方式：直接告知修改意见，我将更新方案

⚠️ 在您明确确认前，我绝对不会执行任何代码变更。
⚠️ 即使多轮讨论，我也必须等待您的明确确认指令。
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 3.2 阻断条件检查

**满足以下任一才能进入阶段4：**
- [ ] "确认" / "ok" / "开始" / "没问题" / "执行"
- [ ] "就这样" / "可以了" / "done"

**不满足的情况（继续等待）：**
- 用户提出修改 → 更新方案 → **重新进入 WAITING_CONFIRM**
- 用户追问问题 → 回答问题 → **重新进入 WAITING_CONFIRM**
- 用户未明确表态 → 继续等待，再次提醒确认

---

## 【阶段4】IMPLEMENT - 技术实现

**进入条件**：用户已明确确认方案

### 4.1 实施前声明

```
[阶段: IMPLEMENT]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚀 开始技术实施

确认状态：✅ 用户已确认
实施方案：{方案标题}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 4.2 按步骤实施

**实施前必须加载：**
- `.easy-coding/RULES.md` - 确保编码规范始终生效

**每完成一步必须输出：**
```
[阶段: IMPLEMENT]

✅ 步骤 {N}/{总} 完成：{步骤描述}
   文件：{文件路径}
   变更：{简要说明}
   规范检查：{是否遵守 RULES.md 中的【强制】规范}
```

**编码规范约束：**
- 所有代码必须遵守 `RULES.md` 中的【强制】规范
- 若因特殊情况需要违反，必须说明原因并获得用户同意
- 若 `RULES.md` 中无相关规范，按通用最佳实践执行

### 4.3 实施中的变更控制

**⚠️ 绝对约束：无论变更大小，必须重新出方案并等待用户确认**

实施过程中，如果用户提出任何变更（哪怕是修改一个变量名、调整一行代码）：

1. **立即停止当前实施**
2. **输出变更方案**：说明变更内容、影响范围、修改方式
3. **返回 WAITING_CONFIRM 阶段**：等待用户明确确认
4. **用户确认后**：按新方案继续实施

**禁止行为（违反即任务失败）：**
- ❌ 未经确认擅自执行任何代码变更
- ❌ 以"改动很小"为由跳过确认流程
- ❌ 在汇报中隐藏已执行的变更

**正确流程示例：**
```
用户：把变量名 userName 改成 username

AI：[阶段: IMPLEMENT]
    收到变更请求。正在输出变更方案...
    
    [阶段: ANALYSIS]
    ## 变更方案：修改变量名
    
    ### 变更内容
    - 文件：UserService.java
    - 原变量：userName
    - 新变量：username
    
    ### 影响范围
    - 本文件内 3 处引用需要同步修改
    - 无外部接口影响
    
    [阶段: WAITING_CONFIRM] ⏸️ 阻断状态
    请确认此变更方案。

用户：确认

AI：[阶段: IMPLEMENT]
    ✅ 变更完成：变量名 userName → username
       文件：UserService.java
       修改位置：第 15、28、42 行
```

### 4.4 实施完成

**用户说以下话语时，视为完成：**
- "完成" / "done" / "ok" / "没问题" / "可以了"

```
[阶段: IMPLEMENT]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 技术实施完成

变更文件汇总：
1. {文件路径}：{变更说明}
2. {文件路径}：{变更说明}

即将进入阶段5：短期记忆生成
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 【阶段5】MEMORY_SHORT - 短期记忆生成

**进入条件**：实施完成

### 5.1 生成记忆文件

**文件名规则**：`{序号}_{日期}_{智能命名}.md`
- 序号：3位数字，从001开始递增
- 示例：`001_20260324_新增用户登录功能.md`

**序号获取**：扫描 `.easy-coding/memory/short/`，取最大序号+1

### 5.2 记忆内容格式

```markdown
# {智能命名}

> 序号：{XXX}
> 日期：{YYYY-MM-DD HH:MM}
> 任务类型：{新功能/Bug修复/重构/性能优化}

## 任务概述
{2-3句话}

## 技术方案摘要
{核心思路，100字内}

## 变更内容
| 文件 | 变更类型 | 说明 |
|---|---|---|
| {路径} | {新增/修改/删除} | {说明} |

## 遇到的问题与解决方案
{若有，记录；无则填"无"}

## 技术决策记录
{若有，记录；无则填"无"}

## 未尽事项
{若有TODO；无则填"无"}

## 关联记忆
- 前置：{前置任务序号}
- 后续：{后续任务序号}
```

### 5.3 短期记忆生成完成

```
[阶段: MEMORY_SHORT]

✅ 短期记忆已生成：{文件名}

即将自动执行：长期记忆沉淀检查（若短期记忆超过10条则自动沉淀）
```

---

## 【阶段6】MEMORY_LONG - 长期记忆沉淀

**进入条件**：短期记忆生成完成后**自动触发**（若短期记忆总数≥10条），或用户主动触发

**主动触发方式**：用户说"沉淀记忆" / "整理记忆" / "归档记忆"

### 6.1 沉淀范围

**自动沉淀条件**：
- 扫描 `.easy-coding/memory/short/` 目录
- 若短期记忆文件总数 **≥ 10 条** → 触发自动沉淀
- 处理最早的若干条，使剩余短期记忆 ≤ 5 条

**处理对象**：最早的短期记忆文件（按序号排序）

**判断方法**：
- 按序号从小到大排序（001, 002, 003...）
- 保留最新的 5 条
- 将超出的（第6条及更早的）纳入沉淀范围

### 6.2 执行步骤

1. 扫描 `.easy-coding/memory/short/` 目录
2. 统计短期记忆文件总数
3. **自动沉淀逻辑**：
   - 若总数 < 10 条 → 输出"短期记忆不足10条，暂不沉淀" → 进入 COMPLETE
   - 若总数 ≥ 10 条 → 按序号排序，保留最新的5条，将超出的纳入沉淀范围
4. 读取符合条件的短期记忆，提取核心信息：
   - 架构决策
   - 技术选型
   - 可复用经验
   - 业务规则
5. 合并到 `.easy-coding/memory/long/MEMORY.md`
6. **删除已沉淀的短期记忆文件**
7. 输出沉淀摘要

### 6.3 长期记忆内容规范

**长度限制：≤ 5000 字符**

**超出时淘汰规则**：
- 优先淘汰最旧的"已淘汰记录"
- 其次淘汰最旧的"可复用经验"
- 最后淘汰最旧的"业务规则"
- **架构决策和技术选型永久保留**（除非被新的决策覆盖）

### 6.4 输出

**有沉淀时：**
```
[阶段: MEMORY_LONG]

📚 长期记忆已更新
- 归档短期记忆：{N} 条（创建超过1天）
- 更新章节：
  - 架构决策：{X} 条
  - 技术选型：{Y} 条
  - 可复用经验：{Z} 条
  - 业务规则：{W} 条
- 已删除短期记忆文件：{文件名列表}
- 当前长期记忆长度：{L} 字符 / 5000 字符

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎉 本次任务流程全部完成！

阶段回顾：
✅ INIT - 初始化
✅ ANALYSIS - 需求分析
✅ WAITING_CONFIRM - 方案确认
✅ IMPLEMENT - 技术实现
✅ MEMORY_SHORT - 短期记忆生成
✅ MEMORY_LONG - 长期记忆沉淀
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[阶段: COMPLETE]
```

**无沉淀时：**
```
[阶段: MEMORY_LONG]

📚 长期记忆检查完成
- 短期记忆共 {N} 条，不足 10 条，暂不沉淀
- 长期记忆未更新

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎉 本次任务流程全部完成！

阶段回顾：
✅ INIT - 初始化
✅ ANALYSIS - 需求分析
✅ WAITING_CONFIRM - 方案确认
✅ IMPLEMENT - 技术实现
✅ MEMORY_SHORT - 短期记忆生成
✅ MEMORY_LONG - 长期记忆沉淀（无需处理）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[阶段: COMPLETE]
```

---

# 附录：模板文件

模板文件位于 `templates/` 目录，初始化时自动拷贝到 `.easy-coding/`：

- `templates/SOUL.md` → `.easy-coding/SOUL.md`
- `templates/RULES.md` → `.easy-coding/RULES.md`
- `templates/ABSTRACT.md` → `.easy-coding/ABSTRACT.md`
- `templates/MEMORY.md` → `.easy-coding/memory/long/MEMORY.md`
