# 编码规范

> 本文件由 AI 根据项目实际编程语言自动生成。
> 若语言识别有误，请手动调整。

---

## 项目语言检测

AI 初始化时请检测项目主要编程语言：
- **Java / Kotlin** → 生成 JVM 语言规范
- **Python** → 生成 Python 规范  
- **JavaScript / TypeScript** → 生成 JS/TS 规范
- **Go** → 生成 Go 规范
- **Rust** → 生成 Rust 规范
- **C / C++** → 生成 C 规范
- **其他语言** → 生成通用规范

---

## 通用编码原则（所有语言适用）

### 【强制】

- **命名清晰**：禁止使用拼音和无意义的 a/b/temp
- **单一职责**：函数/方法不宜过长（建议不超过 80 行）
- **禁止循环内耗时操作**：数据库查询、远程调用等移出循环
- **禁止魔法值**：必须定义为常量或枚举
- **必要注释**：非直观业务规则、兼容/兜底逻辑、协议字段映射、异常处理、幂等/并发/缓存、配置 key 含义、风险或易错边界必须补充必要注释，禁止逐行注释和无信息量注释
- **文档完整**：对外契约、公共 API、复杂业务规则必须有文档或注释说明
- **错误处理**：必须处理，禁止空 catch/except 块
- **敏感信息**：禁止在日志/代码中硬编码密码、密钥

### 【推荐】

- **DRY 原则**：相同逻辑不重复超过 3 次
- **条件清晰**：复杂条件提取为语义清晰的布尔方法
- **空安全**：优先使用语言原生的空安全特性
- **日志规范**：关键节点必须有日志，异常必须有 ERROR 日志

---

## {语言名} 专属规范

> AI 根据检测到的语言，在此处生成具体的命名规范、代码结构规范等

### 命名规范【强制】

（AI 根据语言填充，如：
- Java: 类名 UpperCamelCase，方法 lowerCamelCase
- Python: 函数 lower_case_with_underscores
- Go: 使用驼峰命名，公开首字母大写
- ...）

### 代码结构【强制】

（AI 根据语言填充，如：
- Java: 方法不超过 80 行，参数不超过 5 个
- Python: 遵循 PEP8，函数不超过 50 行
- Go: 函数简短，错误处理显式返回
- ...）

### 文件编码规范【强制】

- 修改已有文件前必须识别原文件编码，写回时保持原编码，永远不要擅自转换 GBK / UTF-8 等编码类型
- 新建文件必须先分析项目编码依据，包括构建配置、编辑器配置、同目录同类文件和项目历史文件；确认后套用项目编码
- 若项目编码证据冲突或无法确认，必须在分析方案的“改动范围”中标注“待用户确认”，不得直接实施
- 用户可在方案确认前针对任一文件的编码字段提出修改；实施时必须严格遵守最终确认的编码字段
- 涉及中文注释或非 ASCII 内容时，必须确认目标文件编码可表达这些字符，避免引入编译或运行时编码错误

### 注释规范【强制】

默认要求（所有语言适用）：
- 新增或修改注释时，注释语种必须匹配当前对话语种；无法识别对话语种时默认使用简体中文；若用户明确指定注释语言，以用户要求为准
- 不得在中文对话中默认写英文注释，除非用户明确要求英文或项目规范硬性要求英文
- 必须优先为非直观业务规则、兼容/兜底逻辑、协议字段映射、异常处理、幂等/并发/缓存、配置 key 含义、风险或易错边界补充注释
- 注释只解释意图、约束、边界、风险和易错点，不解释显而易见的语法动作
- 新增或重构复杂逻辑时，必须同步补齐必要注释，保证后续维护者能快速理解
- 修改完成后必须回看 diff 做注释语种与必要性自检；若未新增注释，必须确认并说明本次改动没有必要注释

### 新文件署名【强制】

- 新建文件若因项目模板、同类文件惯例或用户要求需要作者署名，必须写为 `${Agent Name} with Easy Coding`
- `${Agent Name}` 由当前宿主 Agent 替换为自己的名称，例如 `Codex with Easy Coding`
- 不强制所有新文件添加作者头；仅约束确实需要作者字段或作者注释的场景

（AI 根据语言填充，如：
- Java: 对外 API、接口契约和复杂业务方法补充 Javadoc
- Python: 对外模块、公共函数和复杂逻辑使用 docstring
- Go: 导出类型、导出函数和复杂业务逻辑使用 // 注释
- ...）

### 异常/错误处理【强制】

（AI 根据语言填充，如：
- Java: 禁止空 catch，必须打印堆栈
- Python: 使用 try/except，禁止裸 except
- Go: 显式处理 error，禁止忽略
- Rust: 使用 Result/Option，禁止 unwrap 滥用
- ...）

### 日志规范【强制】

（AI 根据语言填充，如：
- Java: 使用 @Slf4j
- Python: 使用 logging 模块
- Go: 使用标准库 log 或第三方库
- ...）

---

## 项目专属规范

（在此补充项目特定规范，如：
- 特定框架的使用约定
- 团队特有的编码习惯
- 禁止使用的 API 或模式
- ...）
