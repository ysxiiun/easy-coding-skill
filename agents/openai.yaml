interface:
  display_name: "Easy Coding"
  short_description: "固定 Guard + Standard 的轻量单 Skill，共享项目知识、记忆与 Canonical"
  default_prompt: >-
    使用 `$easy-coding` 按固定 Guard 审批语义和 Standard QUALITY 双门处理编程需求；不依赖 Harness 运行时，通过共享知识、记忆、单一 Canonical 消费闭包和受控 writer 原地协作。

policy:
  allow_implicit_invocation: false
